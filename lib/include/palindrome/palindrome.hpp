#pragma once

#include <string>

namespace palindrome {

// Placeholder entry point so the library has something to build and test
// against. The real PAL encode/decode surface will replace this.
[[nodiscard]] std::string greeting();

} // namespace palindrome
